# Docs

Longer-form reference material linked from individual skills — gotchas, connector setup
steps, and worked examples that would clutter a `SKILL.md` if inlined.

Add one file per topic and link to it from the relevant skill rather than duplicating
content across skills.
